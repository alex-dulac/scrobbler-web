export class Song {
    constructor(
        public title: string,
        public artist: string,
        public album: string,
    ) {
        this.title = title;
        this.artist = artist;
        this.album = album;
    }
}